"use client";

/**
 * 标签件的公开入口（历史路径）：<TagBadge /> 与 <TagEditor /> 各自单文件，
 * 这里只做再导出，保持 `@/components/task/tag-badges` 的既有导入不变。
 */
export { TagBadge } from "./tag-badge";
export { TagEditor } from "./tag-editor";
