# DS 体检报告（第一层 · 确定性脚本）

> 生成：2026-09-26T10:47:02.404Z · **状态：provisional（签字点 ① 未签，基准未经人工确认）**

| 检查 | 规则 | 状态 | 违规 | 摘要 |
|---|---|---|---|---|
| `ds-check-tokens` | R3 R4 R5 R6 | ✅ exit 0 | 1132 处 | 阻断级违规共 1132 处 / 115 个文件（存量基线棘轮：未恶化） |
| `ds-check-imports` | R1 R2 | ✅ exit 0 | 0 处 | 违规共 0 处（存量基线棘轮：未恶化） |
| `ds-check-vars` | R7 R8(脚本侧) | ✅ exit 0 | 6 处 | 未注册变量违规 6 处；零引用注册 Token 31 个（废弃候选，不阻断） |

- 存量债策略：基线棘轮（tokens/imports/vars-baseline.json），只许减不许增；新增违规即失败。
- R9（令牌三方一致）由 `bun test` 与 `bun run audit:tokens` 把管，不在本报告内。
- 第二层（独立检查 Agent 视觉/交互报告）应以同日期前缀存档于本目录。
